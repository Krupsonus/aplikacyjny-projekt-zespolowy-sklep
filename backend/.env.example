# Copy this file to .env for local development (outside Docker)
# When running via docker compose, these are set in docker-compose.yml

DATABASE_URL=postgresql://techshop:techshop_pass@localhost:5432/techshop
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
